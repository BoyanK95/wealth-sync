export interface PortfolioItem {
  averagePrice: number;
  currentPrice: number;
  frontend?: string;
  fxPpl: number;
  initialFillDate: string;
  maxBuy: number;
  maxSell: number;
  pieQuantity: number;
  ppl: number;
  quantity: number;
  ticker: string;
}
